# Copy to keystore.properties (git-ignored) and fill in. Never commit the real file.
# storeFile: full path (keep the keystore OUTSIDE the project) or relative to the android/ folder.
storeFile=C:/Users/DEIN-NAME/keystores/concept-coach-upload.jks
storePassword=CHANGE_ME
keyAlias=concept-coach-upload
keyPassword=CHANGE_ME
